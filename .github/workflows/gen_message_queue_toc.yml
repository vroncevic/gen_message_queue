name: gen_message_queue_toc
on:
  push:
    branches: [ master ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: gen_message_queue_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4